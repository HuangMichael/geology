spring.mvc.view.prefix=/WEB-INF/pages/
spring.mvc.view.suffix=.jsp
application.message=Hello Spring boot
spring.datasource.url = jdbc:postgresql://localhost:5432/gdmsdb
spring.datasource.username=sde
spring.datasource.password=lanzhou2018
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1
spring.jpa.properties.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL9Dialect
# Show or not log for each sql query
spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update
spring.http.multipart.maxFileSize=10Mb
spring.http.multipart.maxRequestSize=50Mb
# Naming strategy
spring.jpa.hibernate.naming-strategy=org.hibernate.cfg.ImprovedNamingStrategy
# stripped before adding them to the entity manager)
spring.datasource.max-wait=10000000
spring.datasource.initial-size=5
spring.datasource.validation-query=SELECT  1
spring.jackson.serialization.fail-on-empty-beans=false
#################################################################################
server.jsp-servlet.init-parameters.development=true
########################################################
## 加入AOP配置
########################################################
spring.aop.auto=true
########################################################
###REDIS (RedisProperties) redis基本配置；
########################################################
# REDIS (RedisProperties)
# Redis数据库索引（默认为0）
spring.redis.database=0
# Redis服务器地址

#spring.redis.host=localhost
spring.redis.host=localhost
#spring.redis.host=58.213.65.98

# Redis服务器连接密码（默认为空）
spring.redis.password=
# 连接池最大连接数（使用负值表示没有限制）
spring.redis.pool.max-active=8  
# 连接池最大阻塞等待时间（使用负值表示没有限制）
spring.redis.pool.max-wait=-1  
# 连接池中的最大空闲连接
spring.redis.timeout=100000
spring.redis.pool.min-idle=8
spring.redis.pool.max-idle=50
########################################################
########################################################
log4j.appender.A1.Encoding=UTF-8  
########################################################
hibernate.temp.use_jdbc_metadata_defaults=false
